version: '3.8'

services:
  smartmob-frontend:
    build: 
      context: .
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL:-https://localhost:7052}
    container_name: smartmob-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
