version: '3.8'

services:
  smartmob-frontend:
    build: 
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:5065}
    container_name: smartmob-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
