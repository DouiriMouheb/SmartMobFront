version: '3.8'

services:
  smartmob-frontend:
    build: .
    container_name: smartmob-frontend
    ports:
      - "3000:80"
    environment:
      - VITE_API_BASE_URL=http://localhost:5000
    restart: unless-stopped
